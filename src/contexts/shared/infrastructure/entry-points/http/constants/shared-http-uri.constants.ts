/**
 * Enum representing shared HTTP URI constants.
 */
export enum SharedHttpUriConstants {
    /**
     * URI for the health check endpoint.
     */
    HEALTH_CHECK_URI = '/health-check',

    /**
     * Base URI for the shared HTTP entry point.
     */
    PREFIX = 'shared',
}
