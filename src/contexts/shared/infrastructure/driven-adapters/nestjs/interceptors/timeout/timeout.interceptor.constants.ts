/**
 * Constants used in the timeout interceptor.
 */
export enum TimeoutInterceptorConstants {
    /**
     * Metadata key to cancel the timeout interceptor.
     */
    CANCEL_TIMEOUT_METADATA_KEY = 'CANCEL_TIMEOUT_METADATA_KEY',
    /**
     * Metadata key for storing timeout configuration.
     */
    TIMEOUT_METADATA_KEY = 'TIMEOUT_METADATA_KEY',
}
